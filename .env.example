# PostgreSQL Database Connection String (Optional)
# The application works out-of-the-box in standalone mode with an in-memory catalog
# if DATABASE_URL is not set.
DATABASE_URL="postgres://username:password@localhost:5432/drev_luxury"
